---
const { sketches = [] } = Astro.props;

const featuredSketches = sketches
  .filter((sketch: any) => sketch.featured === true)
  .sort(
    (a: any, b: any) =>
      new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime()
  )
  .slice(0, 4);
---

<section class="w-full">
  <div class="flex flex-col gap-3 sm:mx-auto">
    <div class="flex items-center justify-between text-human-2 border-b-human-3 border-b-2">
      <h3 class="text-4xl font-medium text-left font-bebas">Sketches</h3>
      <a href="/sketches" class="underline">
        Ver todos
      </a>
    </div>

    {featuredSketches.length > 0 ? (
      <ul class="flex flex-col sm:grid sm:grid-cols-4 gap-3 px-3">
        {featuredSketches.map((sketch: any) => (
          <li>
            <a href="/sketches" class="group block focus:outline-none focus-visible:ring-2 focus-visible:ring-human-3" aria-label={`Ver sketch ${sketch.title}`}>
              <article class="flex flex-col gap-2">
                <div class="relative aspect-[4/5] overflow-hidden rounded">
                  <img src={sketch.image} alt={sketch.alt} class="absolute inset-0 h-full w-full object-cover transition-transform duration-200 group-hover:scale-105" loading="lazy" />
                </div>

                <p class="font-bellota text-sm leading-4 text-human-2">
                  {sketch.title}
                </p>
              </article>
            </a>
          </li>
        ))}
      </ul>
    ) : (
      <p class="px-3 text-sm text-human-2">
        No hay sketches destacados todavia.
      </p>
    )}
  </div>
</section>
