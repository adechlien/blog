---
---

<section class="w-full">
    <div class="flex flex-col gap-3 sm:mx-auto">
        <h3 class="text-4xl font-medium text-left font-bebas text-white border-b-white/40 border-b-2">Mural de frases</h3>
        <!-- <ul class="grid grid-cols-2 sm:grid-cols-6 gap-3 px-3">
            {albums.map((album) => (
            <li class="">
                <a href={album.link} target="_blank" class="group block">
                    <article class="flex flex-col justify-center gap-0 overflow-hidden">
                        <img src={`/albums/${album.cover}`} alt={album.title} class="inset-0 w-full h-full object-cover"/>
                        <span class="font-bebas text-xl text-white">{album.title}</span>
                        <span class="font-bellota text-xs text-white">{album.artist}</span>
                    </article>
                </a>
            </li>
            ))}
        </ul> -->
    </div>
</section>
